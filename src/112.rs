/// Calculate increasing and decreasing numbers and filter from the total
/// set of numbers.
///
/// This way, we do not need to iterate over all numbers, but instead just
/// increasing/decreasing numbers.

fn main() {
}
